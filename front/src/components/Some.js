import React from 'react';

const Some = () => {

    let numbers = [1, 2, 3, 4, 5]
    let total = 0
    i = 0
     function avargeValue(numbers) {
        var avarageValue = 0;
        if (numbers.length = 0) {
            for (let i = 0; i < numbers.length; i++) {
                total += numbers[i]
            }
            return avarageValue
        }
     }

     let average = avargeValue(numbers)

    return (
        <div>

        </div>
    );
};

export default Some;